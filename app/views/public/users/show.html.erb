<div class="container">
  <div class="d-flex flex-column flex-md-row">
    <!--左側:プロフィール-->
    <div class="col-md-3">
      <%= render 'pf_box', user: @user %>
    </div>

    <div class="col-md-9">
      <!-- 右側:投稿一覧 -->
      <%= render 'index', episodes: @episodes, user: @user %>
    </div>
  </div>
</div>