<h1>Notifications</h1>
<ul>
  <% @notifications.each do |notification| %>
    <% message = notification.to_notification.message %>
    <% url = notification.to_notification.url %>
    <li>
      <% if notification.type == "CommentNotification" %>
        <%= link_to message[:user], url[:user] %>さんがあなたの投稿<%= link_to message[:episode], url[:episode] %>にコメントしました
      <% elsif notification.type == "RelationshipNotification" %>
        <%= link_to message[:user], url[:user] %>さんがあなたをフォローしました
      <% elsif notification.type == "Favouriteotification" %>
        <%= link_to message[:user], url[:user] %>さんがあなたの投稿<%= link_to message[:post], url[:post] %>にいいねしました
      <% end %>
      <% if notification.read? %>
        <span class="badge badge-success">Read</span>
      <% else %>
        <span class="badge badge-danger">Unread</span>
      <% end %>
    </li>
  <% end %>
</ul>